# 开发与协作指南

本项目是一个小型学习项目，不是生产级商业应用。开发过程应保持简单、易懂、渐进。

## 保持简单

不要把项目当作大型专业软件来设计。除非当前功能确实需要，否则避免创建大量目录、分层、抽象、接口、配置文件、辅助模块或架构模式。

优先选择：

- 更少的文件与目录；
- 直接、清晰的实现；
- 易懂的命名；
- 小范围、可验证的改动。

项目应更像一个组织良好的学生项目，而不是企业级系统。一个功能能合理地放在一两个文件中时，不要为了架构纯度拆成六个文件。

## 学习是目标的一部分

实现新功能时：

1. 简要说明将要修改什么；
2. 用简单语言解释涉及的重要概念及其必要性；
3. 实现最小合理版本；
4. 完成后说明改动内容、重要文件和手动验证方法。

不要假设用户已经熟悉高级 Windows API、桌面应用架构、依赖注入、设计模式或渲染系统。引入新概念时，说明它为什么需要。

## 避免过早架构

不要为了假想的未来需求提前建设插件系统、依赖注入框架、复杂 provider、事件总线、repository 层或大型抽象层次。

只有在当前代码出现真实复杂度时才引入抽象：先写简单代码，复杂度出现后再重构。

在 SLC 完成前，不要设计整个未来应用。

## 测试

早期阶段不要求大规模自动化测试。不要主动创建大型测试目录、mock 系统、集成测试基础设施、CI 流水线或复杂测试框架。

当前以基本手动测试为主。实现功能后，提供简短的验证步骤，例如：

1. 运行应用；
2. 在资源管理器中选中一个 JPG；
3. 按下 `Space`；
4. 确认预览出现；
5. 按下 `Esc`；
6. 确认预览关闭。

项目稳定或复杂度提高后，再按实际需要引入自动化测试。

## 渐进开发

不要一次实现完整路线图，只实现当前请求的功能。例如，任务是预览一个 JPG 时，只实现该能力；不要同时加入 Markdown、导航、PDF、设置、主题、日志、安装器或未来功能。

每个步骤都应足够小，让用户能理解其中发生的变化。

## 大改动前

如果实现需要很多新文件、重大架构调整、新框架、大型依赖或替换现有子系统，先解释原因。多个方案可选时，优先选择最简单可用的方案。

## 代码与工具约束

- 使用 C#、.NET 10 LTS、WPF 与 `dotnet` CLI。
- 不依赖 Visual Studio、Visual Studio Designer 或只能通过 GUI 完成的项目配置。
- 所有必要配置必须保存在可提交的文本文件中。
- 优先可读性，避免为了炫技使用复杂语言特性或模式。
- 注释只解释重要或不寻常的逻辑，不逐行复述显而易见的代码。

## 当前心态

> 先做一个小东西 → 让它工作 → 理解它 → 再改进它。

而不是先设计生产级架构、实现全部未来需求或提前优化。


# 开发进度

用这个文件记录当前开发切片、已完成事项和下一步。它不替代 [SLC.md](SLC.md)：SLC 定义产品范围，这里记录实际进展。

## 当前：V0.1 JPG 快速预览核心切片（已完成）

- [x] Step 0：WPF 窗口能跑
  - [x] 创建最小 `.NET 10 + WPF` 项目。
  - [x] `dotnet run` 能弹出一个简单窗口。
  - [x] 按 `Esc` 能关闭窗口。
  - [x] 不读取文件、不监听 `Space`、不显示图片。

- [x] Step 1：窗口能显示 JPG
  - [x] 给定一个本地 JPG 路径，窗口能正确显示图片。
  - [x] 图片不会明显拉伸变形。
  - [x] 暂不读取 Explorer 的选中项。

- [x] Step 2：能读取 Explorer 选中的 JPG
  - [x] 用户先在 Explorer 中选中一个 JPG。
  - [x] 程序能取得该选中项并显示对应图片。
  - [x] 暂不监听全局 `Space`。

- [x] Step 3：`Space` 触发预览
  - [x] Explorer 位于前台且选中 JPG 时，按 `Space` 打开预览。
  - [x] 预览打开后，按 `Space` 或 `Esc` 关闭。
  - [x] 不在其他应用中错误触发。

完成 Step 3 后，V0.1 完成。它验证 JPG 快速预览的核心交互，但不是 [SLC.md](SLC.md) 定义的完整 SLC。

## 已完成

- [x] 明确开发技术栈：Windows 11、Zed、C#、.NET 10 LTS、WPF 与 `dotnet` CLI。
- [x] 整理项目文档：README、SLC 与开发协作规则。
- [x] 将开发约束放入 `AGENTS.md`。
- [x] 完成 V0.1 Step 0：最小 WPF 窗口可启动并可用 `Esc` 关闭。
- [x] 完成 V0.1 Step 1：窗口可显示给定路径的 JPG，并保持图片比例。
- [x] 完成 V0.1 Step 2：无参数启动时，可读取 Explorer 选中的 JPG 并显示预览。
- [x] 完成 V0.1 Step 3：在 Explorer 中按 `Space` 打开 JPG 预览，并可用 `Space` 或 `Esc` 关闭。
- [x] 完成 V0.1：JPG 快速预览核心切片。

## 下一步：V0.2 图片预览体验

目标：在不改变 Explorer + `Space` 核心交互的前提下，完善静态图片预览的格式支持、缩放和初始窗口尺寸。

- [x] Step 1：JPG/JPEG 的缩放与窗口尺寸
  - [x] 图片保持原始宽高比例，不拉伸、不裁切。
  - [x] 小图片不会打开过大的窗口。
  - [x] 大图片不会超出屏幕可用区域。
  - [x] 宽图、竖图和方图均有合理的初始窗口尺寸。

- [x] Step 2：PNG 预览
  - [x] Explorer 中选中 PNG 后，按 `Space` 能显示预览。
  - [x] PNG 透明区域保持透明，显示为窗口背景色。
  - [x] PNG 使用与 JPG/JPEG 相同的缩放和窗口尺寸规则。

- [x] Step 3：WEBP 预览
  - [x] Explorer 中选中 WEBP 后，按 `Space` 能尝试显示预览。
  - [x] Windows 已安装 WebP 编解码器时，正常预览。
  - [x] 编解码器不可用时，显示清楚的提示，不崩溃。
  - [x] 不为此引入第三方图片解码依赖。

- [x] 完成 V0.2：静态图片预览体验。

V0.2 暂不包含：鼠标滚轮缩放、缩放滑条、旋转、EXIF 信息、缩略图列表、GIF 动画或图片导航。

两个问题：
1. 按照示例图片，大小大概是 1.3 MB，打开速度很快；但是我用相机拍的照片差不多 10 MB，打开速度就有点慢。
2. 竖屏照片被横过来了，也就是它的旋转是错误的。

功能想法：
1. 预览时 ctrl c，右键可以直接复制
2. 固定预览窗口的位置
3. 预览窗口，鼠标滚轮
4. 放大缩小

## V0.3：相机照片正确性与速度（已完成）

目标：让真实相机照片以正确方向快速打开，同时保持当前 JPG/JPEG、PNG、WEBP 的预览体验。

- [x] Step 1：EXIF 方向
  - 竖拍 JPEG 依照照片的 EXIF 方向正确显示。
  - 横拍 JPEG 的现有显示不受影响。
  - 用一张竖拍手机或相机照片手动验证。

- [x] Step 2：大图片打开速度
  - 先用同一张相机照片记录当前打开体验，确认问题与图片像素尺寸有关，而不只看文件 MB 数。
  - 按实际预览尺寸解码大图片，避免为屏幕外的像素付出不必要的解码和内存成本。
  - 确认缩小解码后，预览窗口内没有明显模糊或变形。
  - 当前取舍：为提升打开速度，大图会按预览尺寸解码，因此预览可用像素低于原图；正常大小的预览优先速度。以后加入放大查看时，再考虑按更高分辨率重新加载。

- [x] Step 3：前台显示回归确认
  - 确认预览窗口在 Explorer 前面打开；不强制抢占键盘焦点。
  - 已在关闭并重新启动监听器后手动验证。

- [x] 完成 V0.3：相机照片方向正确、大图预览速度改善、预览窗口稳定显示在 Explorer 上方。

## V0.4：图片预览便捷操作（已完成）

目标：保持当前图片预览体验不变，为当前预览文件增加少量高频操作。

- [x] Step 1：用默认 App 打开
  - 预览窗口打开时，按 `Enter` 用 Windows 默认 App 打开当前文件。
  - 暂不添加按钮；先验证快捷键行为。
- [x] Step 2：复制当前文件
  - 预览窗口打开时，按 `Ctrl+C` 将当前文件复制到剪贴板。
  - 在 Explorer、桌面等位置按 `Ctrl+V`，可粘贴出文件副本。
  - 不复制图片像素，暂不添加右键菜单。
- [x] Step 3：记住预览窗口位置
  - 第一次预览窗口居中。
  - 用户可拖动窗口；关闭预览时保存其位置。
  - 后续预览窗口（包括重启监听器后）在上次位置打开。
  - 不同宽高比的预览窗口使用同一个中心坐标。
  - 只使用一个小型文本设置文件保存坐标，不增加设置页面或复杂配置系统。
- [x] 完成 V0.4：默认 App 打开、复制当前文件和预览窗口位置记忆均已通过手动验证。

## V0.5：基础文本预览（已完成）

目标：让项目从图片预览扩展到最基础的文本文件预览，同时保持实现简单，不引入 Markdown 渲染库、WebView2 或复杂的预览架构。

- [x] Step 1：TXT 预览
  - 在 Explorer 中选中 `.txt` 文件后，按 `Space` 打开预览。
  - 使用只读、可滚动的文本区域显示内容。
  - 使用合理的默认窗口尺寸。
  - 保持 `Space`、`Esc`、`Enter` 和 `Ctrl+C` 的现有行为。
- [x] Step 2：Markdown 原文预览
  - 在 Explorer 中选中 `.md` 文件后，按 `Space` 打开预览。
  - 第一版直接显示 Markdown 原文，不渲染标题、列表等样式。
  - 使用适合阅读 Markdown 和代码片段的等宽字体。
  - 不为此引入第三方 Markdown 依赖。
- [x] Step 3：文本体验与回归确认
  - 常见 UTF-8 和带 BOM 的文本可以正常显示。
  - 空文件和无法读取的文件有简单、清楚的提示。
  - 较长文本可以正常滚动。
  - 图片预览和窗口位置记忆不受影响。
  - 文本文件仍可用默认 App 打开，也可以复制文件本身。

- [x] 完成 V0.5：TXT、Markdown 原文预览和文本体验回归均已通过手动验证；富文本 Markdown 渲染不属于这个版本。

## V0.6：同文件夹文件导航（已完成）

目标：预览打开时，使用 `←` 和 `→` 按 Explorer 当前显示顺序连续浏览同一文件夹中的受支持文件。到达首尾时停止，不循环；切换时复用当前预览窗口。

- [x] Step 1：图片方向键导航
  - 按 `←` 或 `→` 切换同一文件夹中的 JPG、JPEG、PNG 和 WEBP 图片。
  - 只把当前支持的图片格式加入文件序列；最终排序规则在 Step 3 统一为 Explorer 当前显示顺序。
  - 到达第一个或最后一个文件时保持不变，不循环到另一端。
  - 切换时复用当前预览窗口，避免反复关闭和明显闪烁。
- [x] Step 2：图片与文本混合导航
  - 把 TXT 和 Markdown 加入同一个文件序列。
  - 支持类似 `image.jpg → notes.md → another-image.png` 的连续预览。
  - 切换文件类型或图片比例时，正确更新内容和窗口尺寸。
  - 窗口围绕同一个中心位置调整，不因内容宽高比变化而明显偏移。
  - 已手动长按方向键快速浏览包含图片、TXT 和 Markdown 的文件夹，内容、窗口尺寸与位置均正常。
- [x] Step 3：Explorer 同步与回归确认
  - 预览打开时读取 Explorer 当前显示顺序，并在其中跳过暂不支持的格式。
  - Explorer 的选中项跟随当前预览文件。
  - `Enter` 用默认 App 打开当前预览文件。
  - `Ctrl+C` 复制当前预览文件本身。
  - 确认 `Space`、`Esc`、窗口位置记忆和前台显示行为没有回归。
  - 已手动确认方向键导航顺序和 Explorer 最终选中项均正常。

- [x] 完成 V0.6：同文件夹文件导航与 Explorer 同步均已通过手动验证，并完成 [SLC.md](SLC.md) 当前定义的首个 SLC。

## Release 0.1：首次打包与异机验证

目标：停止扩展功能，把已经完成的首个 SLC 制作成可交给其他 Windows 11 用户运行的测试包，并根据真实测试结果只修复阻碍使用的问题。

- [x] Step 1：生成并本机验证发布包
  - 使用 `Release + win-x64 + self-contained` 生成发布文件夹。
  - 目标电脑不需要预先安装 .NET 10 Desktop Runtime。
  - 第一版使用文件夹和 ZIP，不制作安装器，不启用单文件、裁剪或 AOT。
  - 在本机从发布目录直接运行 `QPeek.exe`，确认基本预览闭环正常。
  - 已生成 `QPeek-v0.1.0-preview-win-x64.zip`（70.4 MB），并确认 ZIP 内包含完整发布文件和入口程序。
  - ZIP SHA-256：`9C9C13B4380C983242D6FF1BF106DEE8E44827B97FF547F4BB7B58C4831F841B`。
  - 已在本机手动确认发布版体验与开发测试版本一致。
- [x] Step 2：在另一台 Windows 11 电脑测试
  - 将 ZIP 复制到没有源码和开发环境的 Windows 11 x64 电脑。
  - 解压后直接运行，确认程序能够启动并监听 `Space`。
  - 回归图片、TXT、Markdown、方向键导航、Explorer 同步、`Enter`、`Ctrl+C` 和窗口位置。
  - 记录 SmartScreen、WebP 编解码器、多屏或缩放比例等环境差异；只修复阻碍首个版本使用的问题。
  - [x] 首次异机测试通过：自包含包可在另一台 Windows 11 笔记本正常运行，核心预览体验与开发电脑一致。
  - [x] 首次异机测试发现：程序允许重复启动，且只能通过任务管理器退出，容易产生多个后台监听器。
  - [x] 已加入单实例限制：第二次启动会立即退出，不再创建第二个监听器。
  - [x] 已加入系统托盘图标和 `Exit` 菜单，不再需要通过任务管理器退出。
  - [x] 已对重新发布的 EXE 自动验证：连续启动两次时，第二个实例退出且只保留一个进程。
  - [x] 修复后的 ZIP 已在另一台 Windows 11 笔记本确认：单实例、托盘退出和原有预览功能均正常。
  - 待后续处理：TXT 或 Markdown 预览中选中文字后，左右方向键可能被文本框接收，无法切换文件。
- [ ] Step 3：整理首次公开发布
  - [x] 展示名称确定为 `QPeek`，EXE、窗口标题和托盘提示统一使用该名称。
  - [x] 使用透明多尺寸 `QPeek.ico` 作为 EXE 和系统托盘图标。
  - [x] `QPeek` 自包含包已在另一台 Windows 11 电脑验证：图标清晰，核心功能正常。
  - [x] 已更新 README 的当前功能、运行方法、快捷键、已知限制和停止程序的方法。
  - [x] 已准备 `v0.1.0-preview` 的 GitHub Release 说明、附件名称和文件校验值。
  - [x] 仓库已更名为 `QPeek`，并采用 MIT License 开源。
  - [x] 已确认发布包命名与版本号，创建并验证 `v0.1.0-preview` GitHub Release 草稿。
  - [ ] 将仓库设为 Public，复核草稿后正式发布 Release。
  - 异机测试稳定后，再录制简短演示视频并发布到 GitHub 或自媒体。

## V0.6 之后的候选

- 预览打开时，在 Explorer 中选择其他文件后按 `Space` 直接切换预览内容。
- 鼠标滚轮缩放和平移；放大查看时再考虑加载更高分辨率的图片。
- 右键复制当前文件。
- 视频预览；播放、暂停、进度、声音和系统编解码器支持需要单独规划。
- 复制的是原图，还是，预览图


完成 V0.6 后，SLC 是不是算完成？做第一次打包?，上传其他电脑做测试。也许可以发布第一版了，做个宣传视频，发一下社交媒体

隐藏托盘里，打开 QPeek 调节软件的一些功能，ui颜色，黑色，白色，暗夜模式？快捷键教学等等
试用 oil-oil GitHub README 美化功能，做个美观的 Release 界面

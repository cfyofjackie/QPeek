# 开发进度

用这个文件记录当前开发切片、已完成事项和下一步。它不替代 [SLC.md](SLC.md)：SLC 定义产品范围，这里记录实际进展。

## V0.1：JPG 快速预览核心切片（已完成）

- [x] Step 0：WPF 窗口能跑
  - [x] 创建最小 `.NET 10 + WPF` 项目。
  - [x] `dotnet run` 能弹出一个简单窗口。
  - [x] 按 `Esc` 能关闭窗口。
  - [x] 不读取文件、不监听 `Space`、不显示图片。

- [x] Step 1：窗口能显示 JPG
  - [x] 给定一个本地 JPG 路径，窗口能正确显示图片。
  - [x] 图片不会明显拉伸变形。
  - [x] 暂不读取 Explorer 的选中项。

- [x] Step 2：能读取 Explorer 选中的 JPG
  - [x] 用户先在 Explorer 中选中一个 JPG。
  - [x] 程序能取得该选中项并显示对应图片。
  - [x] 暂不监听全局 `Space`。

- [x] Step 3：`Space` 触发预览
  - [x] Explorer 位于前台且选中 JPG 时，按 `Space` 打开预览。
  - [x] 预览打开后，按 `Space` 或 `Esc` 关闭。
  - [x] 不在其他应用中错误触发。

完成 Step 3 后，V0.1 完成。它验证 JPG 快速预览的核心交互，但不是 [SLC.md](SLC.md) 定义的完整 SLC。

## 已完成

- [x] 明确开发技术栈：Windows 11、Zed、C#、.NET 10 LTS、WPF 与 `dotnet` CLI。
- [x] 整理项目文档：README、SLC 与开发协作规则。
- [x] 整理本地开发与 Coding Agent 协作约束，不纳入公开仓库。
- [x] 完成 V0.1 Step 0：最小 WPF 窗口可启动并可用 `Esc` 关闭。
- [x] 完成 V0.1 Step 1：窗口可显示给定路径的 JPG，并保持图片比例。
- [x] 完成 V0.1 Step 2：无参数启动时，可读取 Explorer 选中的 JPG 并显示预览。
- [x] 完成 V0.1 Step 3：在 Explorer 中按 `Space` 打开 JPG 预览，并可用 `Space` 或 `Esc` 关闭。
- [x] 完成 V0.1：JPG 快速预览核心切片。

## V0.2：图片预览体验（已完成）

目标：在不改变 Explorer + `Space` 核心交互的前提下，完善静态图片预览的格式支持、缩放和初始窗口尺寸。

- [x] Step 1：JPG/JPEG 的缩放与窗口尺寸
  - [x] 图片保持原始宽高比例，不拉伸、不裁切。
  - [x] 小图片不会打开过大的窗口。
  - [x] 大图片不会超出屏幕可用区域。
  - [x] 宽图、竖图和方图均有合理的初始窗口尺寸。

- [x] Step 2：PNG 预览
  - [x] Explorer 中选中 PNG 后，按 `Space` 能显示预览。
  - [x] PNG 透明区域保持透明，显示为窗口背景色。
  - [x] PNG 使用与 JPG/JPEG 相同的缩放和窗口尺寸规则。

- [x] Step 3：WEBP 预览
  - [x] Explorer 中选中 WEBP 后，按 `Space` 能尝试显示预览。
  - [x] Windows 已安装 WebP 编解码器时，正常预览。
  - [x] 编解码器不可用时，显示清楚的提示，不崩溃。
  - [x] 不为此引入第三方图片解码依赖。

- [x] 完成 V0.2：静态图片预览体验。

V0.2 暂不包含：鼠标滚轮缩放、缩放滑条、旋转、EXIF 信息、缩略图列表、GIF 动画或图片导航。

两个问题：
1. 按照示例图片，大小大概是 1.3 MB，打开速度很快；但是我用相机拍的照片差不多 10 MB，打开速度就有点慢。
2. 竖屏照片被横过来了，也就是它的旋转是错误的。

功能想法：
1. 预览时 ctrl c，右键可以直接复制
2. 固定预览窗口的位置
3. 预览窗口，鼠标滚轮
4. 放大缩小

## V0.3：相机照片正确性与速度（已完成）

目标：让真实相机照片以正确方向快速打开，同时保持当前 JPG/JPEG、PNG、WEBP 的预览体验。

- [x] Step 1：EXIF 方向
  - 竖拍 JPEG 依照照片的 EXIF 方向正确显示。
  - 横拍 JPEG 的现有显示不受影响。
  - 用一张竖拍手机或相机照片手动验证。

- [x] Step 2：大图片打开速度
  - 先用同一张相机照片记录当前打开体验，确认问题与图片像素尺寸有关，而不只看文件 MB 数。
  - 按实际预览尺寸解码大图片，避免为屏幕外的像素付出不必要的解码和内存成本。
  - 确认缩小解码后，预览窗口内没有明显模糊或变形。
  - 当前取舍：为提升打开速度，大图会按预览尺寸解码，因此预览可用像素低于原图；正常大小的预览优先速度。以后加入放大查看时，再考虑按更高分辨率重新加载。

- [x] Step 3：前台显示回归确认
  - 确认预览窗口在 Explorer 前面打开；不强制抢占键盘焦点。
  - 已在关闭并重新启动监听器后手动验证。

- [x] 完成 V0.3：相机照片方向正确、大图预览速度改善、预览窗口稳定显示在 Explorer 上方。

## V0.4：图片预览便捷操作（已完成）

目标：保持当前图片预览体验不变，为当前预览文件增加少量高频操作。

- [x] Step 1：用默认 App 打开
  - 预览窗口打开时，按 `Enter` 用 Windows 默认 App 打开当前文件。
  - 暂不添加按钮；先验证快捷键行为。
- [x] Step 2：复制当前文件
  - 预览窗口打开时，按 `Ctrl+C` 将当前文件复制到剪贴板。
  - 在 Explorer、桌面等位置按 `Ctrl+V`，可粘贴出文件副本。
  - 不复制图片像素，暂不添加右键菜单。
- [x] Step 3：记住预览窗口位置
  - 第一次预览窗口居中。
  - 用户可拖动窗口；关闭预览时保存其位置。
  - 后续预览窗口（包括重启监听器后）在上次位置打开。
  - 不同宽高比的预览窗口使用同一个中心坐标。
  - 只使用一个小型文本设置文件保存坐标，不增加设置页面或复杂配置系统。
- [x] 完成 V0.4：默认 App 打开、复制当前文件和预览窗口位置记忆均已通过手动验证。

## V0.5：基础文本预览（已完成）

目标：让项目从图片预览扩展到最基础的文本文件预览，同时保持实现简单，不引入 Markdown 渲染库、WebView2 或复杂的预览架构。

- [x] Step 1：TXT 预览
  - 在 Explorer 中选中 `.txt` 文件后，按 `Space` 打开预览。
  - 使用只读、可滚动的文本区域显示内容。
  - 使用合理的默认窗口尺寸。
  - 保持 `Space`、`Esc`、`Enter` 和 `Ctrl+C` 的现有行为。
- [x] Step 2：Markdown 原文预览
  - 在 Explorer 中选中 `.md` 文件后，按 `Space` 打开预览。
  - 第一版直接显示 Markdown 原文，不渲染标题、列表等样式。
  - 使用适合阅读 Markdown 和代码片段的等宽字体。
  - 不为此引入第三方 Markdown 依赖。
- [x] Step 3：文本体验与回归确认
  - 常见 UTF-8 和带 BOM 的文本可以正常显示。
  - 空文件和无法读取的文件有简单、清楚的提示。
  - 较长文本可以正常滚动。
  - 图片预览和窗口位置记忆不受影响。
  - 文本文件仍可用默认 App 打开，也可以复制文件本身。

- [x] 完成 V0.5：TXT、Markdown 原文预览和文本体验回归均已通过手动验证；富文本 Markdown 渲染不属于这个版本。

## V0.6：同文件夹文件导航（已完成）

目标：预览打开时，使用 `←` 和 `→` 按 Explorer 当前显示顺序连续浏览同一文件夹中的受支持文件。到达首尾时停止，不循环；切换时复用当前预览窗口。

- [x] Step 1：图片方向键导航
  - 按 `←` 或 `→` 切换同一文件夹中的 JPG、JPEG、PNG 和 WEBP 图片。
  - 只把当前支持的图片格式加入文件序列；最终排序规则在 Step 3 统一为 Explorer 当前显示顺序。
  - 到达第一个或最后一个文件时保持不变，不循环到另一端。
  - 切换时复用当前预览窗口，避免反复关闭和明显闪烁。
- [x] Step 2：图片与文本混合导航
  - 把 TXT 和 Markdown 加入同一个文件序列。
  - 支持类似 `image.jpg → notes.md → another-image.png` 的连续预览。
  - 切换文件类型或图片比例时，正确更新内容和窗口尺寸。
  - 窗口围绕同一个中心位置调整，不因内容宽高比变化而明显偏移。
  - 已手动长按方向键快速浏览包含图片、TXT 和 Markdown 的文件夹，内容、窗口尺寸与位置均正常。
- [x] Step 3：Explorer 同步与回归确认
  - 预览打开时读取 Explorer 当前显示顺序，并在其中跳过暂不支持的格式。
  - Explorer 的选中项跟随当前预览文件。
  - `Enter` 用默认 App 打开当前预览文件。
  - `Ctrl+C` 复制当前预览文件本身。
  - 确认 `Space`、`Esc`、窗口位置记忆和前台显示行为没有回归。
  - 已手动确认方向键导航顺序和 Explorer 最终选中项均正常。

- [x] 完成 V0.6：同文件夹文件导航与 Explorer 同步均已通过手动验证，并完成 [SLC.md](SLC.md) 当前定义的首个 SLC。

## Release 0.1：首次打包与异机验证

目标：停止扩展功能，把已经完成的首个 SLC 制作成可交给其他 Windows 11 用户运行的测试包，并根据真实测试结果只修复阻碍使用的问题。

- [x] Step 1：生成并本机验证发布包
  - 使用 `Release + win-x64 + self-contained` 生成发布文件夹。
  - 目标电脑不需要预先安装 .NET 10 Desktop Runtime。
  - 第一版使用文件夹和 ZIP，不制作安装器，不启用单文件、裁剪或 AOT。
  - 在本机从发布目录直接运行 `QPeek.exe`，确认基本预览闭环正常。
  - 已生成 `QPeek-v0.1.0-preview-win-x64.zip`（70.4 MB），并确认 ZIP 内包含完整发布文件和入口程序。
  - ZIP SHA-256：`9C9C13B4380C983242D6FF1BF106DEE8E44827B97FF547F4BB7B58C4831F841B`。
  - 已在本机手动确认发布版体验与开发测试版本一致。
- [x] Step 2：在另一台 Windows 11 电脑测试
  - 将 ZIP 复制到没有源码和开发环境的 Windows 11 x64 电脑。
  - 解压后直接运行，确认程序能够启动并监听 `Space`。
  - 回归图片、TXT、Markdown、方向键导航、Explorer 同步、`Enter`、`Ctrl+C` 和窗口位置。
  - 记录 SmartScreen、WebP 编解码器、多屏或缩放比例等环境差异；只修复阻碍首个版本使用的问题。
  - [x] 首次异机测试通过：自包含包可在另一台 Windows 11 笔记本正常运行，核心预览体验与开发电脑一致。
  - [x] 首次异机测试发现：程序允许重复启动，且只能通过任务管理器退出，容易产生多个后台监听器。
  - [x] 已加入单实例限制：第二次启动会立即退出，不再创建第二个监听器。
  - [x] 已加入系统托盘图标和 `Exit` 菜单，不再需要通过任务管理器退出。
  - [x] 已对重新发布的 EXE 自动验证：连续启动两次时，第二个实例退出且只保留一个进程。
  - [x] 修复后的 ZIP 已在另一台 Windows 11 笔记本确认：单实例、托盘退出和原有预览功能均正常。
  - 后续发现：TXT 或 Markdown 预览中选中文字后，左右方向键可能被文本框接收，无法切换文件；已在 V0.7 Step 3 修复。
- [x] Step 3：整理首次公开发布
  - [x] 展示名称确定为 `QPeek`，EXE、窗口标题和托盘提示统一使用该名称。
  - [x] 使用透明多尺寸 `QPeek.ico` 作为 EXE 和系统托盘图标。
  - [x] `QPeek` 自包含包已在另一台 Windows 11 电脑验证：图标清晰，核心功能正常。
  - [x] 已更新 README 的当前功能、运行方法、快捷键、已知限制和停止程序的方法。
  - [x] 已准备 `v0.1.0-preview` 的 GitHub Release 说明、附件名称和文件校验值。
  - [x] 仓库已更名为 `QPeek`，并采用 MIT License 开源。
  - [x] 已确认发布包命名与版本号，创建并验证 `v0.1.0-preview` GitHub Release 草稿。
  - [x] 仓库已设为 Public，并正式发布 `v0.1.0-preview` Pre-release。
  - [x] 已使用匿名 HTTP 请求确认公众可以访问仓库并下载 Release ZIP。
  - 演示视频不作为首次发布的前置条件；以后可以重新录制并补充到 GitHub 或自媒体。

- [x] 完成 Release 0.1：首个 SLC 已打包、异机验证并公开发布。

## V0.7：快捷键边界与交互收尾（已完成）

目标：优先修复文本预览中已确认的方向键导航问题，再限制预览窗口的全局快捷键作用范围，最后完成一次小范围回归验证。

- [x] Step 1：评估 Explorer 重命名/文本输入状态识别（结论：放弃实现）
  - 已确认现有的前台进程判断可以阻止 QPeek 在其他软件中误触发。
  - 尝试过 UI Automation 编辑控件和 Win32 caret 状态两种判断，都会把 Explorer 中的正常文件选择误判为文本输入，导致核心 `Space` 预览无法打开。
  - 为避免用边缘功能损害核心交互，不保留本 Step 的代码。Explorer 重命名时按 `Space` 可能打开预览，作为已知限制记录。

- [x] Step 2：预览窗口失去前台后不干扰其他软件
  - 这个问题会让 QPeek 抢占其他软件中的全局快捷键，仍有修复必要；它只需判断 QPeek 预览窗口是否位于前台，不需要识别 Explorer 内部的文本输入状态。
  - 没有预览窗口时，Explorer 位于前台且选中受支持文件，`Space` 仍打开预览。
  - 预览窗口位于前台时，QPeek 才处理 `Space`、`Enter` 和 `Ctrl+C`。
  - 预览窗口存在但不在前台时，QPeek 不处理这些按键，也不关闭预览或复制当前预览文件。
  - 方向键只由预览窗口自身处理，不属于本 Step 的全局快捷键边界。
  - 创建预览窗口时保存其 Win32 窗口句柄，关闭时清空；三个全局快捷键处理函数只比较当前前台窗口与这个句柄，不增加其他状态识别。
  - 命令行隐藏窗口测试已确认：修改前后台预览会响应 `Space`、`Enter` 和 `Ctrl+C`，修改后三者均被忽略；项目独立编译为 0 警告、0 错误。
  - 已由用户手动确认：预览窗口在前台时原有三个快捷键正常；切换到其他软件后按键由其他软件处理；重新回到预览后快捷键恢复。

- [x] Step 3：修复文本预览中的方向键导航
  - TXT 或 Markdown 预览中即使文本区域获得焦点或选中了文字，单独按 `←` 和 `→` 仍可切换上一个或下一个受支持文件。
  - `Shift`、`Ctrl` 或 `Alt` 与方向键组成的组合键继续交给文本区域处理，不扩大本次修改范围。
  - 鼠标滚轮、滚动条和文本选择仍可正常使用。
  - `Ctrl+C` 继续按当前产品规则复制文件本身，不改为复制选中文字。
  - 窗口改为在文本框处理按键前接收 `PreviewKeyDown`，避免只读文本框吞掉普通左右键。
  - 已用不显示窗口的临时 WPF 事件测试确认：修改前无法从 `a.md` 切到 `b.md`，修改后可以正常切换；项目独立编译通过。

- [x] Step 4：小型 UI 清理与必要回归验证
  - [x] 正常预览时只在窗口标题栏显示文件名；底部区域只用于空文件、读取失败和复制结果等状态提示。
  - [x] 命令行隐藏窗口测试确认：正常 Markdown 的底部状态已隐藏，空文件和文件不存在提示仍正常显示；项目独立编译为 0 警告、0 错误。
  - [x] 已由用户手动回归图片、TXT、Markdown、方向键导航、Explorer 同步、`Enter`、`Ctrl+C`、窗口位置、单实例和托盘退出。

- [x] 完成 V0.7：文本预览中的方向键导航恢复正常，预览窗口只在自身位于前台时处理现有全局快捷键，并确认现有预览交互没有回归。

V0.7 暂不包含：Explorer 重命名/文本输入状态识别、重做全局快捷键系统、自定义快捷键、完整设置页、新文件格式或图片缩放。

## 当前：V0.8 跟随 Explorer 选择

目标：使用 `Space` 打开预览后，用户继续在对应的 Explorer 窗口中选择其他文件时，QPeek 自动更新当前预览，不需要为每个文件再次按 `Space`。Explorer 保持操作焦点；切换到其他软件后，QPeek 不监视选择，也不干扰按键。

- [x] Step 1：监听 Explorer 选择并更新预览
  - 只在预览已打开、最初对应的 Explorer 窗口位于前台时检查选择变化。
  - 第一版使用简单的低频定时检查，不引入 UI Automation 事件、后台服务或复杂监听架构。
  - Explorer 单选另一个受支持的图片、TXT 或 Markdown 时，复用当前预览窗口直接显示新文件。
  - 多选、无选择、不支持的格式或仍为当前文件时，保持现有预览不变。
  - QPeek 自己用 `←` / `→` 同步 Explorer 选择时，不重复加载同一个文件。
  - 预览期间使用 250ms 的 WPF 定时器；只有对应 Explorer 位于前台时才读取单选项，预览关闭或程序退出时停止定时器。
  - 命令行隐藏窗口测试已确认：选择不同受支持文件时复用窗口并刷新导航；同一文件和不支持格式不会重复加载；项目独立编译为 0 警告、0 错误。
  - 已由用户手动确认：鼠标选择其他受支持文件时内容会自动更新；预览内容切换正常。点击 Explorer 后预览会被窗口盖住，留给 Step 2 处理。

- [x] Step 2：保持 Explorer 焦点与预览可见
  - 用户在 Explorer 中连续选择文件时，QPeek 更新内容但不抢走 Explorer 的键盘焦点。
  - 预览只在 QPeek 或对应 Explorer 位于前台时保持对用户可见；切换到其他软件后不继续遮挡其他窗口。
  - 不使用永久 `Topmost`，不扩大到多窗口管理或复杂层级规则。
  - 第一次尝试通过 `SetWindowPos` 定时抬高普通窗口；手动测试确认 Explorer 激活后仍会盖住 QPeek，因此已移除该方案。
  - 当前实现改为在 QPeek 首次显示前，把对应 Explorer HWND 设为原生 owner；由 Windows 保证预览位于该 Explorer 上方，同时保持 Explorer 可以获得输入焦点。
  - QPeek 仍不是永久 `Topmost`；切换到其他软件时，其他软件可以正常盖住 Explorer 与预览。
  - 命令行回归检查已确认 owner 在 HWND 创建和显示前建立、旧 `SetWindowPos` 方案已移除；独立输出构建为 0 警告、0 错误。
  - 已由用户手动确认：重叠场景下预览保持在对应 Explorer 上方，新的 owner 方案通过。

- [x] Step 3：`Space` 开关与回归验证
  - 预览打开且对应 Explorer 位于前台时，按 `Space` 关闭当前预览。
  - 其他软件中的 `Space`、`Enter` 和 `Ctrl+C` 继续由当前软件处理。
  - 手动回归鼠标选择、Explorer 方向键选择、图片与文本切换、不同文件夹、多选、不支持格式、QPeek 左右导航、Explorer 同步、`Enter` 和 `Ctrl+C`。
  - 已实现：预览打开时，只有 QPeek 自己或最初对应的 Explorer 位于前台，`Space` 才关闭预览并被 QPeek 消费；其他窗口中的 `Space` 保持原行为。
  - `Enter` 与 `Ctrl+C` 的现有前台判断未改动；命令行边界检查通过，独立输出构建为 0 警告、0 错误。
  - 已由用户手动确认：`Space` 开关及相关回归正常，Step 3 通过。

- [x] 完成 V0.8：预览会跟随对应 Explorer 的单选文件直接更新，同时保持清楚的前台、可见性与快捷键边界。

V0.8 暂不包含：监视多个 Explorer 窗口、文件系统变化监听、悬停预览、永久置顶、新文件格式、图片缩放或设置开关。

## 当前：V0.9 Markdown 渲染

目标：Markdown 文件不再只显示原文，而是在 QPeek 内使用原生 WPF 文档控件显示常用 Markdown 样式；TXT 预览保持不变，并继续兼容 Explorer 跟随、文件导航和现有快捷键。

- [x] Step 1：建立原生 Markdown 渲染基础
  - 使用稳定版 `MdXaml` 将 Markdown 字符串转换为 WPF `FlowDocument`，不引入 WebView2 或浏览器运行时。
  - `.md` 使用独立的可滚动文档控件；`.txt` 继续使用现有只读文本框。
  - 第一版验证标题、段落、粗体、斜体、列表、引用、行内代码和代码块等常用语法。
  - Markdown 转换失败时回退显示原文，不让单个文档导致预览失败。
  - 已实现：项目引用稳定版 `MdXaml 1.27.0`，Markdown 使用独立的 `FlowDocumentScrollViewer`，TXT 保持现有文本框；渲染异常时显示等宽原文和状态提示。
  - 命令行隐藏窗口测试已确认 Markdown 会生成非空 `FlowDocument`、TXT 不会进入渲染控件；项目独立构建为 0 警告、0 错误。
  - 已由用户手动确认：标题、列表、引用、强调和代码等基础 Markdown 内容可以正常预览，Step 1 通过。

- [x] Step 2：整理样式与内容边界
  - 调整正文、标题、列表、引用和代码块的间距、字体及配色，保持简单、清晰、适合快速阅读。
  - 明确链接的显示与点击行为，并验证中文、长文档、长代码行和格式不完整的文档。
  - 暂不专门支持内嵌图片、远程内容、原始 HTML、Mermaid、数学公式、语法高亮或原文/渲染切换。
  - 已完成基础版本：采用 `MdXaml` 自带的 GitHub-like 样式，并统一使用适合中英文阅读的 `Segoe UI` 正文字体、15 号字号和文档留白；代码块继续使用等宽字体和独立背景。
  - 链接保留可识别样式与目标提示；只有用户点击 `http` 或 `https` 链接时才交给 Windows 默认浏览器，本地路径、相对地址和其他协议不会执行。
  - Markdown 不从当前工作目录解析相对资源；内嵌图片和其他扩展内容仍不作为本 Step 的支持范围。
  - 命令行隐藏窗口测试已确认：中文和格式不完整的 Markdown 可以生成文档；长代码行不会启用横向滚动；链接目标提示和链接命令边界符合预期。项目独立构建为 0 警告、0 错误。
  - 截图对比后确认：基础渲染已经可用，但内置 GitHub-like 样式只能作为起点，列表结构和整体排版距离成熟编辑器的 Markdown 预览仍有明显差距，因此本 Step 暂不勾选。
  - [x] 修复嵌套列表层级：当前两个空格缩进的子列表可能被 `MdXaml` 渲染成同一级列表。只规范化送入渲染器的文本，不修改原始 Markdown 文件；处理时避开围栏代码块，避免改变代码内容。
  - 已实现：渲染前识别当前列表实际使用的缩进单位，并转换成 `MdXaml` 可稳定识别的四空格层级；两个空格和标准四空格写法都能保持正确层级，原始文件不会被修改。
  - 命令行隐藏窗口测试已确认：父列表、子列表和更深一层列表会生成对应的嵌套 `FlowDocument` 列表；有序列表同样适用，围栏代码块中的列表形文本保持原样。临时输出构建为 0 警告、0 错误。
  - 已由用户手动确认：两个空格缩进的说明项恢复为子列表，第一个待办通过。
  - [x] 支持只读任务列表：将 `- [x]` 和 `- [ ]` 显示为已选中或未选中的方框，去掉任务项前多余的普通圆点，并保留父子任务缩进；预览中的方框不可编辑原文件。
  - 已实现：渲染完成后将任务标记替换成不可点击、不可聚焦的原生 WPF 方框；包含任务项的列表不再显示额外圆点，混合列表中的普通项目则保留一个轻量圆点。
  - 命令行隐藏窗口测试已确认：已完成和未完成状态正确，原始 `[x]` / `[ ]` 不再显示；嵌套任务、任务与普通项目混合的列表、纯普通列表均保持各自标记，预览不会修改原始文件。临时输出构建为 0 警告、0 错误。
  - 已由用户手动确认：只读任务方框、嵌套层级和普通列表标记显示正常，第二个待办通过。
  - [x] 建立简单的 QPeek Markdown 样式，不再把 `MdXaml` 内置样式直接作为最终外观：正文使用柔和深灰而非纯黑，H1/H2 降低字重并拉开层级，在主要标题下增加浅色细分隔线。
  - 已实现：样式集中保存在 `MainWindow.xaml`，正文改为柔和深灰并使用浅灰白背景；H1/H2/H3/H4 使用递减字号和半粗字重，H1/H2 增加浅灰底部分隔线，不再直接使用 `MdXaml` 的内置 GitHub-like 文档样式。
  - 命令行隐藏窗口测试已确认：QPeek 文档样式会应用到生成的 `FlowDocument`；正文颜色、背景色、H1/H2/H3 字号与字重、H1/H2 分隔线均符合设定，只读任务方框没有回归。临时输出构建为 0 警告、0 错误。
  - 已由用户手动确认：正文、背景、标题层级和分隔线显示效果良好，第三个待办通过。
  - [x] 调整阅读节奏：增加标题前后、段落之间和列表组之间的留白，将左右文档留白调整到约 28–32，并使用适合连续阅读的行高；避免标题、正文和列表紧贴在一起。
  - 已实现：在现有预览控件外边距内增加 18 的文档左右留白，使内容距离窗口两侧合计约 30；正文使用 24 行高和 11 的段后距，H1–H4 分别设置适合其字号的行高与前后留白，列表组增加底部间距和轻度缩进。
  - 命令行隐藏窗口测试已确认：文档留白、正文行高、段落间距、H1/H2 间距和嵌套列表组间距均应用到实际 `FlowDocument`，只读任务方框没有回归。临时输出构建为 0 警告、0 错误。
  - 新增宽屏阅读要求：Markdown 预览窗口较宽或最大化时，正文保持左对齐，但整列正文应限制最大宽度并在窗口中水平居中；普通大小窗口继续自适应，不固定成窄栏。
  - 已实现：Markdown 阅读栏最大宽度为 960，并在可用区域内水平居中；预览区域不足 960 时自动缩小到当前可用宽度。阅读栏之外使用相同背景色，避免最大化后出现突兀的色块边界。
  - 命令行隐藏布局测试已确认：1600 宽区域内正文栏宽 960、左右各留 320；800 宽区域内正文栏自动扩展到可用的 776、左右各留 12，普通窗口没有被强制缩窄。临时输出构建为 0 警告、0 错误。
  - 已由用户手动确认：正文行高、段落和列表间距显示正常；Markdown 最大化后正文栏居中，普通窗口仍能自适应，第四个待办通过。
  - [x] 整理细节样式：行内代码和代码块使用较浅背景及合适内边距；引用使用灰色文字和左边框；链接使用柔和蓝色并在悬停时提供反馈；普通列表圆点保持低视觉重量。
  - 实现边界：继续使用原生 WPF `FlowDocument` 和现有 `MdXaml`，先通过一个小型列表预处理和一套集中样式完成改进，不为外观调整引入 WebView2、完整 Markdown AST 或新的渲染架构。
  - 已实现：行内代码使用较小的 Consolas 字体和浅灰背景；围栏代码块及缩进代码块会正确命中 `MdXaml` 实际生成的只读代码控件，增加内边距、浅灰背景并对长行自动换行；引用、链接及悬停反馈沿用集中样式，普通无序列表统一为更轻的实心圆点。
  - 命令行隐藏窗口测试已确认：行内代码、两类代码块、引用、链接、普通列表和任务列表均应用预期样式，长代码行启用换行，只读任务方框没有回归。项目独立构建为 0 警告、0 错误。
  - 已由用户手动确认：细节样式显示正常，Step 2 通过。

- [x] Step 3：导航与快捷键回归
  - 回归 Markdown 文本选择和滚动，以及 `←` / `→`、`Space`、`Enter`、`Ctrl+C`。
  - 回归 Explorer 鼠标选择跟随、图片/TXT/Markdown 互相切换、窗口层级和关闭行为。
  - 命令行隐藏窗口测试已确认：Markdown 允许文本选择，长文档可以滚动；`←` / `→` 可在 Markdown 与 TXT 间往返；Explorer 跟随的核心切换可在 Markdown、TXT 和图片间往返，重复选择和不支持格式不会替换当前预览；`Esc` 仍能关闭预览。
  - 快捷键边界检查已确认：其他软件位于前台时，`Space`、`Enter` 和 `Ctrl+C` 不会被 QPeek 消费。此轮未发现需要修改生产代码的回归，项目独立构建为 0 警告、0 错误。
  - 手动验证范围：在真实 Explorer 中用鼠标和方向键切换 Markdown、TXT、图片，检查重叠窗口层级；分别验证 `Space` 关闭、`Enter` 交给默认应用、`Ctrl+C` 复制当前文件，以及 Markdown 鼠标选文和滚轮滚动。
  - 已由用户手动确认：真实使用未发现问题，导航、文件切换、窗口层级和快捷键回归通过，Step 3 完成。

- [x] 完成 V0.9：Markdown 常用内容可在原生 WPF 控件中清晰渲染，失败时安全回退原文，现有预览交互没有回归。

V0.9 暂不包含：Markdown 内嵌图片、远程资源、原始 HTML、Mermaid、数学公式、语法高亮、编辑能力、原文/渲染切换或完整主题系统。

## Release 0.2：Markdown 渲染与 Explorer 跟随

目标：将 V0.7–V0.9 的交互修复、Explorer 选择跟随和 Markdown 渲染整理为第二个公开版本 `v0.2.0`。

- [x] 版本号确定为 `v0.2.0`，继续提供 Windows 11 x64 自包含 ZIP，不制作安装器、自动更新或代码签名。
- [x] README 与 Release Notes 更新为当前功能、下载名称和已知限制，项目程序集版本写入 `0.2.0`。
- [x] 生成并核验 `QPeek-v0.2.0-win-x64.zip`，记录文件大小和 SHA-256。
  - 发布目录共 476 个文件；ZIP 逐项核对后 476/476 个文件名称和大小一致，压缩包大小 72.9 MiB。
  - ZIP SHA-256：`7381B1611E21CB250DFA873DF1483363512C3981764EDE3A56C1135CBB0B7A83`。
- [x] 提交并推送发布资料，创建 `v0.2.0` GitHub Release 并上传 ZIP。
  - `v0.2.0` 标签、标题、Release Notes 和 `QPeek-v0.2.0-win-x64.zip` 已发布；该版本未标记为 Pre-release。
- [x] 匿名验证 Release 页面及附件可以公开访问。
  - 未使用 GitHub 登录信息的 HTTP 请求已确认 Release 页面返回 `200 text/html`，附件下载最终返回 `200 application/octet-stream`。

- [x] 完成 Release 0.2：`v0.2.0` 正式版及 Windows 11 x64 自包含 ZIP 已公开发布。

## 计划：V0.10 视频预览

V0.9 完成后再单独切片视频功能。第一版优先评估 WPF `MediaElement` 和 Windows 系统编解码能力，只承诺经过实测的少量格式；播放、暂停、进度、声音、文件切换、失败提示和资源释放需要分别明确，不与 Markdown 同时开发。

## V0.9 之后的候选

### 近期：优先考虑

- 在 Windows 10 实机上验证能否启动和完成核心预览闭环；验证前不在 README 中承诺支持。
- 补充英文 README 和简短演示 GIF，并继续改善 GitHub README 与 Release 页面的展示。

### 中期：完成近期项目后再选

- 为当前文件添加右键菜单，第一版可只包含“复制文件”。
- 小型设置入口：深色/浅色外观、快捷键说明和必要的开关；不提前建设完整设置架构。
- 图片切换动画；只在不影响预览速度和连续导航时添加。
- 独立使用说明。优先放在 README 或发布包内，不默认在解压后强制弹出。

### 远期：单独规划

- 图片缩放和平移；当前大图会按预览尺寸降采样，等能够按缩放级别重新解码更高分辨率并处理内存与取消加载后再实现。
- 文件夹预览；先明确要展示的内容和对导航行为的影响。
- 独立网站、社交媒体宣传和 Buy Me a Coffee；等项目有持续用户反馈后再投入。
- npm 不是 Windows 桌面程序的自然分发方式，暂不作为下载渠道。

备注：当前 `Ctrl+C` 复制的是原始文件本身，不是窗口内的预览图像。首个 SLC、首次异机测试和 `v0.1.0-preview` 公开发布均已完成。
